/*Anotações no notion e no whimsical 

    - Um escopo de bloco delimita a área de funcionamento de uma variável (como se fossem variáveis locais) que ficam inacessíveis fora do escopo em que elas foram criadas
    - Em uma função, às vezes usamos a palavra return para que o que foi processado dentro dela fique disponível fora dela.
    - O return acaba com o processamento da função
    - Variáveis criadas em escopos locais funcionam neles e para os filhos
    - Variáveis criadas em escopo global são acessíveis por, por exemplo, funções que possuem escopo de acesso restrito a quem faz parte dele
    - O nome do parâmetro de uma função pode variar desde que o nome das variáveis (que estão no escopo global) que vão passar informações para ela seja o mesmo 
    - Se não for passado parâmetro para a função o nome da variável usada no escopo de bloco de um for, por exemplo, que busque infos externas  (alunos.length), deve ser o mesmo da variável que está em escopo global e passar parâmetro na chamada da função não faz diferença
*/